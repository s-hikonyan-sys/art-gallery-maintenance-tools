# Secrets API 暗号化シークレットファイル（ローカル開発用テンプレート）
# このファイルは make gen-secrets を実行すると自動的に生成されます
# 手動でコピーする場合は以下の手順を参照してください：
#
#   cp secrets.yaml.encrypted.example secrets.yaml.encrypted
#   その後 make gen-secrets を実行して実際の暗号化値を生成する

database:
  password: "encrypted:（make gen-secrets を実行して設定）"
