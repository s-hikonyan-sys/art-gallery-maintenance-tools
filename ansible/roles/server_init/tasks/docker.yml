---
# Docker CE と Docker Compose プラグインのインストール（AlmaLinux 10）

- name: Remove old Docker packages if present
  ansible.builtin.dnf:
    name:
      - docker
      - docker-client
      - docker-client-latest
      - docker-common
      - docker-latest
      - docker-latest-logrotate
      - docker-logrotate
      - docker-engine
      - podman
      - runc
    state: absent
  tags:
    - init-docker

- name: Add Docker CE repository
  ansible.builtin.yum_repository:
    name: docker-ce-stable
    description: Docker CE Stable - $basearch
    baseurl: "{{ docker_repo_url }}/$releasever/$basearch/stable"
    gpgcheck: yes
    gpgkey: "{{ docker_repo_url }}/gpg"
    enabled: yes
  tags:
    - init-docker

- name: Install Docker CE and Compose plugin
  ansible.builtin.dnf:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present
  tags:
    - init-docker

- name: Enable and start Docker service
  ansible.builtin.systemd:
    name: docker
    enabled: yes
    state: started
    daemon_reload: yes
  tags:
    - init-docker

- name: Add app user to docker group
  ansible.builtin.user:
    name: "{{ app_user }}"
    groups: docker
    append: yes
  tags:
    - init-docker

- name: Add SSH admin user to docker group (release-tools Ansible / docker login)
  ansible.builtin.user:
    name: "{{ admin_user }}"
    groups: docker
    append: yes
  tags:
    - init-docker

- name: Verify Docker is running
  ansible.builtin.command: docker info
  register: docker_info_result
  changed_when: false
  tags:
    - init-docker

# release-tools 等の Ansible が community.docker を使う際、対象ホストの Python に
# requests / docker が必要。AlmaLinux 10 では dnf に該当パッケージがないため pip で入れる。
- name: Install python3-pip for Ansible Docker module dependencies
  ansible.builtin.dnf:
    name: python3-pip
    state: present
  tags:
    - init-docker

- name: Install Python libraries for Ansible Docker modules (requests, docker)
  ansible.builtin.pip:
    name:
      - requests
      - docker
    executable: pip3
  tags:
    - init-docker

- name: Show Docker version
  ansible.builtin.debug:
    msg: "Docker installed successfully. Run 'docker info' for details."
  tags:
    - init-docker
